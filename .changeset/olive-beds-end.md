---
"ovr": patch
---

Fix `Schema.optional()`, `Schema.nullable()`, and `Schema.nullish()` types for newer TypeScript inference.

Fix JSX declaration emit for newer TypeScript versions so `ovr` builds without private field export errors.
